MES AI

Objective
I want to build a free and open source Manufacturing Execution System (MES) that will be designed, coded and tested solely by an AI (you).  
The code should never need to be looked at or maintained by a human, but rather by an AI coding agent.  
Therefore the implementation should be optimized for ease of maintenance and enhancements by an AI, not by a human.

The duration of the project will be many weeks and will cross sessions, therefore there must be a procedure in place to resume work where a previous session left off without needing to understand the implementation from scratch.  
Also, I need to be able to refer to a module of functionality by an identifier for enhancements rather than describe what it does to save time and token expense.

The project deliverable will be an MES framework with plug-in functionality (much like a coding IDE has plugins).  
An end user will download the framework from GitHub and proceed to customize the functionality based on their unique requirements.  
Using their preferred IDE (e.g. Claude Code, Visual Studio Code, etc.), they will describe to the AI agent what customizations they require, and the AI will proceed to implement them.

The first task is to create and document a project plan describing step-by-step what implementation tasks will be accomplished.  
I will decide if a task has been completed and we will move on to the next task.

Features
A MES is software used in a factory to produce goods effectively.  
It interfaces to an Enterprise Resource Planning (ERP) at inbound touchpoints (e.g. production orders, and manufactured materials) as well as at outbound touchpoints (e.g. work in process reporting and consumption of raw materials).  
An MES also interfaces to production equipment controllers (e.g. Programmable Logic Controllers or PLCs and microcontrollers) as well as test equipment for test results.

An MES tracks the progress of work-in-process (WIP) represented by units or lots of material through the physical model of production lines, work centers and equipment.  
A dispatching algorithm upon the occurance of a production event decides what work center or equipment to move the work (unit or lot) to.  
The dispatching could be manual in which case an operator enters a disposition mapped to a destination.  
Or the dispatching could be automated based on the downstream equipment�s capability and capacity to receive additional work.

The end user will use an IDE such as Claude Code or VS Code to implement new features.  We will use VS Code to develop the MES framework.  
A Git repository will be used to store all artifacts from which the end user will download the framework and proceed to make their customizations.

Implementation
The major phases to accomplish the goals of this project are:
!1)	Survey existing commercial MES�s and their capabilities and create a list of required functionality, then document the conclusions.
(2)	Propose an implementation architecture.  The system should be client/server with a REST HTTP/HTTPS interface between clients and a server.  
The clients can be written in the programming language of the end user�s choice.  
A runtime client that manipulates WIP can have a GUI for use by a human or can be headless if interfacing to automated equipment for example.  
A design time client will allow the end user to view what funtionality has been implemented and how the pieces �fit together�.  
The server will provide MES capabilities and store data in a Relational Database Management System (RDBMS) for tracking and reporting purposes.
(3)	Divide up the implementation into a series of manageable implementation tasks starting with the server. 
Automated testing needs to be implemented to verify that a task has been complemented.  
For the physical environment of an ERP, controllers or test equipment, it is not feasible to test against �real� interfaces or devices.  In this case, mocking up an interface will be required.


How to resume a future session: Say "Resume MES AI project" — I'll read PROJECT_STATE.json and SESSION_LOG.md to pick up exactly where we left off.

How It Works
Session start: You paste a single prompt like "Resume MES AI project" — the AI reads PROJECT_STATE.json and SESSION_LOG.md, instantly knows the current state, and picks up where it left off.
During session: As work progresses, both files are updated incrementally.
Session end: A final update captures exactly where we stopped and what's next.
Module IDs: Every functional module gets a short identifier (e.g., WIP-TRACK, DISPATCH-ENGINE, ERP-ADAPTER) registered in PROJECT_STATE.json. You reference these IDs instead of describing functionality.

PostGreSQL Docker Quick start in WSL2:
cd /mnt/c/dev/mes_ai/server
chmod +x scripts/dev-setup.sh
./scripts/dev-setup.sh

Or manually step by step:
docker compose up -d                          # Start PostgreSQL
cp .env.example .env                          # Create env config
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"                       # Install MES server
uvicorn mes.main:app --reload --port 8000     # Run server

If you're in a PowerShell terminal on Windows instead of bash/zsh, you can also just run the setup directly:
cd c:\dev\mes_ai\server
.venv\Scripts\activate
pip install -e ".[dev]" --quiet

Docker desktop for Windows: kentra52@gmail.com/Aligner2024 use Gmail account

PostGreSQL: database: mes_ai, postgres/postgres
postgresql+asyncpg://postgres:postgres@localhost:5432/mes_ai

To activate the Python virtual environment:
  source .venv/bin/activate

To run the MES server in Windows:
  uvicorn mes.main:app --reload --host 0.0.0.0 --port 8000
to step FastAPI dev server: Press Ctrl+C in the terminal where it's running.

To run tests:
  python -m pytest tests/unit/ -v

DT-CLIENT:

Start the server: cd server && uvicorn mes.main:app --reload
Start the client: cd clients/design_time && npm run dev
Open http://localhost:5173/uom

################### Feb. 25th ##############################
1.  Install Python dependencies
cd c:\dev\mes_ai\server
pip install -e ".[dev]"
2. Generate & run the initial Alembic migration
The alembic/versions/ folder is currently empty — no migration has been generated yet (tests used in-memory SQLite). I just fixed env.py to include all Layer 0-3 model imports. Now:
cd c:\dev\mes_ai\server
alembic revision --autogenerate -m "initial schema layers 0-3"
alembic upgrade head
3.  (Optional) Seed UoM data
cd c:\dev\mes_ai\server
python scripts/seed_uom.py
4.   Start the FastAPI server
cd c:\dev\mes_ai\server
$env:MES_AUTH_MODE = "none"
uvicorn mes.main:app --reload --port 8000

health:
Invoke-RestMethod -Uri http://localhost:8000/health

To re-enable auth later, just remove the env var or set $env:MES_AUTH_MODE = "local".

API docs will be at http://localhost:8000/api/v1/docs.

5.  Start the DT-CLIENT dev server
cd c:\dev\mes_ai\clients\design_time
npm run dev
Open http://localhost:5173 — Vite proxies /api requests to the FastAPI server on port 8000.

to get the rest api token in local mode:
$token = (Invoke-RestMethod -Method POST -Uri http://localhost:8000/api/v1/auth/local/login -ContentType "application/json" -Body '{"username":"admin","password":"admin"}').access_token
$token

################## Feb. 26 ############################
dev-setup.sh script:
PostgreSQL connectivity check — probes localhost:5432 via psql or a Python socket fallback
Three run modes: dev-setup.sh (setup + start), --setup (install only), --start (launch only)
Alembic migrations run automatically during setup
DT-CLIENT npm install during setup
Launches both services: MES server on :8000 + DT-CLIENT on :5173 as background processes
Ctrl+C trap cleanly kills both processes
Node.js detection alongside Python

PostgreSQL server:
Created pg-service.ps1. Usage (from an admin PowerShell):

Command	Effect
.\pg-service.ps1 install	Init data dir, register Windows service, start, create mes_ai database
.\pg-service.ps1 start	Start the service
.\pg-service.ps1 stop	Stop the service
.\pg-service.ps1 restart	Stop + start
.\pg-service.ps1 status	Show service state, port, and PG version
.\pg-service.ps1 uninstall	Stop, remove service, optionally delete data
The service is named postgresql-mes (auto-start), data lives in server\pgdata, and it auto-discovers PG binaries from PATH or common install locations.

################# Feb 27 ##############
to initialize the database:
alembic upgrade head
to check current version:
alembic current
to create default UOMs:
cd c:\dev\mes_ai\server
python scripts/seed_uom.py

For Windows PowerShell, there's no combined script — you start each separately:
# Terminal 1: MES server
cd c:\dev\mes_ai\server
# alembic upgrade head (if schema change)
# python scripts/seed_uom.py (for new data)
$env:MES_AUTH_MODE = "none"
uvicorn mes.main:app --reload --port 8082

Terminal 2 — Register plugins (one time) and enable/disable
cd c:\dev\mes_ai\server
c:\dev\mes_ai\.venv\Scripts\Activate.ps1
python -m mes.cli --server http://localhost:8082 plugin install sap-erp-simulator
python -m mes.cli --server http://localhost:8082 plugin enable sap-erp-simulator
python -m mes.cli --server http://localhost:8082 plugin disable sap-erp-simulator

python -m mes.cli --server http://localhost:8082 plugin install oracle-erp-simulator
python -m mes.cli --server http://localhost:8082 plugin enable oracle-erp-simulator
python -m mes.cli --server http://localhost:8082 plugin disable oracle-erp-simulator

python -m mes.cli --server http://localhost:8082 plugin install packml-availability
python -m mes.cli --server http://localhost:8082 plugin enable packml-availability

python -m mes.cli --server http://localhost:8082 plugin install semi-e10-availability
python -m mes.cli --server http://localhost:8082 plugin enable semi-e10-availability

python -m mes.cli --server http://localhost:8082 plugin install stomp-jms
python -m mes.cli --server http://localhost:8082 plugin enable stomp-jms

Terminal 3 — ERP Simulator GUI:
cd c:\dev\mes_ai\clients\erp_simulator
npm install
npm run dev

Then open http://localhost:5174 — click Dashboard → "Check Health" → both adapters should show green.

# Terminal 4: DT-CLIENT
cd c:\dev\mes_ai\clients\design_time
npm run dev

# Terminal 5: Equipment availability simulator
cd c:\dev\mes_ai\clients\availability_simulator
npm install
npm run dev
serves on http://localhost:5175

# Runtime client
cd c:\dev\mes_ai\clients\run_time
npm run dev
Then open http://localhost:5176.

Release a created order 
For a released order, create a lot via the RT-CLIENT's Orders page (click an order → "Create Lot"),

product name:  mes.ai, MES.ai, MES.AI